name: Demo WorkFlow
on: workflow_dispatch
jobs:
  Test:
    runs-on: ubuntu-latest
    steps:
      - name: get the code
        run: |
          echo  "AWS KEY === ${{ secrets.AWS_ACCESS_KEY_ID }}"
          echo  "AWS SECRETS === ${{ secrets.AWS_SECRET_ACCESS_KEY }}"
           
             
