name : Run the API Automation Test 
on: workflow_dispatch
jobs:
  Demo:
   runs-on: ubuntu-latest
   steps:
    - name: Get Code 
      uses: actions/checkout@v4
    - name: Run Test
      run: mvn clean test 
    